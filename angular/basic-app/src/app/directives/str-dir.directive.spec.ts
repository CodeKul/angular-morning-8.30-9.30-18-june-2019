import { StrDirDirective } from './str-dir.directive';

describe('StrDirDirective', () => {
  it('should create an instance', () => {
    const directive = new StrDirDirective();
    expect(directive).toBeTruthy();
  });
});
